# STEP1 후보 — 노이즈 필터 이후 depth cap + enrich_budget (admin 키워드 소싱)
# source_group = CID, depth = naver_categories.full_path 깊이

discovery_post_filter:
  enabled: true
  enrich_budget: 400
  by_depth:
    L1: 50
    L2: 60
    L3: 70
    L4: 80
  default_depth_label: L2
  depth_ge5_uses: L4
  dedupe: deepest_win
